spring:
  application:
    name: neuron-cloud
  config:
    import: optional:classpath:config/application-liteflow.yml
  profiles:
    active: '@profile.name@'

minidao:
  base-package: com.echarge.minidao.dao